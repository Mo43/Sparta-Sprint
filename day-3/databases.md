## Databases Options Challenge

Research relational and non-relational databases and address the following points

What is a structure of the Database

Relational databases store data in tables (also called relations) made up of rows and columns similar to a spreadsheet.
it is structured tables with rows and colums linked by common keys allowing efficient retrieval and storage. 

Each table represents an entity, and relationshps between tables are established through keys

What is a structure of the Database - 
Primary Keys: Unique identifiers for each row in a table
Foreign Keys: Fields that reference primary keys in other tables, creating relationships
Schema: A predefined structure that must be established before data entry, specifying data types, constraints, and relationships



When would it be used? 


Comparison of the two databases 

Examples of the databases


